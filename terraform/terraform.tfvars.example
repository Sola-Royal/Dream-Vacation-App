# Copy to terraform.tfvars (gitignored) and fill in for local runs.
# In CI/CD this is passed via -var="key_pair_name=..." from the EC2_KEY_PAIR_NAME secret instead.

key_pair_name    = "your-existing-aws-key-pair-name"
ssh_allowed_cidr = "0.0.0.0/0" # tighten to your IP/32 if possible
